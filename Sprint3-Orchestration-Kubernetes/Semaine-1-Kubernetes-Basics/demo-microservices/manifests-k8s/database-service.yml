apiVersion: v1
kind: Service
metadata:
  name: database-service
  labels:
    app: users-app
    resource-type: service
    tier: db-backend
spec:
  selector:
    app: users-app
    tier: db-backend
  type: ClusterIP
  ports:
    - port: 3002
      targetPort: 3002
