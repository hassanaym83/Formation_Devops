apiVersion: v1
kind: Service
metadata:
  name: validation-service
  labels:
    app: users-app
    resource-type: service
    tier: backend
spec:
  selector:
    app: users-app
    tier: backend
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
