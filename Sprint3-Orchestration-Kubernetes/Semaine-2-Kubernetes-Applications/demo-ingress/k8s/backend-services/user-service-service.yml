apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: blood-donation
  labels:
    project: blood-donation
    app: user-service
    component: api
    tier: backend
    resource-type: service
spec:
  type: ClusterIP
  selector:
    app: user-service
    component: api
    tier: backend
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001